{
  "id": "FEAT-002",
  "type": "feat",
  "description": "The article editor: a WordPress-style create/edit screen at /admin/articles/new and /admin/articles/[id]/edit with every article field (title, auto slug, excerpt, author picker, category picker, publishedAt, SEO title/description, FAQs, featured + inline images), a rich-text body editor producing valid Portable Text, and image upload to Sanity assets as the logged-in user. Depends on FEAT-001 (auth gate, admin client, portable-text utils).",
  "status": "completed",
  "steps": [
    "Create app/admin/articles/new/page.tsx and app/admin/articles/[id]/edit/page.tsx. Both render a shared components/admin/ArticleEditor.tsx client component; the edit page passes the document id so the editor loads the existing doc (prefer the draft `drafts.<id>` if it exists, else the published doc) via the admin client and hydrates the internal model using the parsers from lib/portable-text.ts.",
    "In ArticleEditor.tsx build a Studio-like two-column layout: main column = Title (large input; slug auto-derives from title using a slugify that mirrors Sanity's behavior - lowercase, spaces->hyphens, strip non-url chars, maxLength 96, with a manual edit/override field), Excerpt (textarea, live char count, max 300 enforced), Body editor (FEAT-003 supplies the table part; this feature supplies the rich-text part), FAQs (repeatable {question, answer} rows with add/remove). Right sidebar = Publish panel (FEAT-003 wiring), Featured Image uploader, Author picker, Category picker, Published At (datetime-local), SEO Title (max 70, char count), SEO Description (max 160, char count). Match the field set + validation limits in sanity/schemas/article.ts exactly.",
    "Build the rich-text body editor in components/admin/RichTextEditor.tsx ('use client'). Prefer a dependency-free contentEditable-based editor (no new npm dep, given INTEGRATIONS_ONLY): a toolbar with Normal/H2/H3/H4/Quote block styles, Bold/Italic/Underline/Code inline marks, bullet + numbered list, Link (prompt for href, stored as a link annotation), and an 'Insert image' + 'Insert table' action. On every change, serialize the editor content to the Portable Text array using lib/portable-text.ts builders (blocks with correct style, spans with marks arrays, link markDefs with _key referenced by span marks, list blocks with listItem+level:1, image blocks, tableBlock). Generate _key for every block/span/markDef. Ensure output round-trips through components/news/PortableTextRenderer.tsx identically (h2-h4, blockquote, strong/em/underline/code, links, bullet/number lists, inline images with alt+caption). If a contentEditable approach proves too fragile to serialize reliably, an acceptable alternative is a structured block-list editor (each block is a typed row: paragraph/heading/quote/list/image/table with its own controls) that maps 1:1 to Portable Text - this is often MORE reliable for exact-shape output; choose whichever yields correct, valid Portable Text and document the choice.",
    "Implement author + category pickers as select/combobox components (components/admin/ReferencePicker.tsx). Fetch options with the admin client: authors `*[_type=='author']|order(name asc){_id, name}`, categories `*[_type=='category']|order(title asc){_id, title}`. Store the selection as a Sanity reference { _type:'reference', _ref:<_id> } (matches article.ts author/category reference fields). Show a hint when no authors/categories exist yet (must be created in Studio or a future admin screen).",
    "Implement image upload in components/admin/ImageUploader.tsx used for BOTH the featured image (mainImage: image + alt) and inline body images (image + alt + caption, options hotspot). Upload the file via the authenticated admin client: client.assets.upload('image', file) which runs as the logged-in user; on success store the returned asset as { _type:'image', asset:{ _type:'reference', _ref: result._id }, alt, caption? }. Show a preview using the existing urlFor()/@sanity/image-url builder (from lib/sanity.ts) or the returned asset url. Handle upload errors and large files gracefully. Featured image stores alt only; inline images store alt + optional caption to match article.ts.",
    "Wire client-side validation matching the schema: title required, slug required, excerpt required (<=300), author required, category required, publishedAt required; seoTitle<=70, seoDescription<=160; FAQ question+answer required when a FAQ row exists. Show inline errors and disable Save/Publish until required fields are valid. Do not yet perform the actual write (that is FEAT-003) - expose an onSave(model, mode) callback that FEAT-003 implements, but structure the component so save wiring drops in cleanly."
  ],
  "acceptance_criteria": [
    "/admin/articles/new renders an empty editor; /admin/articles/[id]/edit loads an existing article (draft preferred) and hydrates all fields including body blocks, table blocks, images, author, category, FAQs.",
    "Title auto-generates an editable slug; excerpt/SEO title/SEO description enforce 300/70/160 char limits with visible counts.",
    "Rich-text editor can produce every supported block/mark: H2/H3/H4, blockquote, bold/italic/underline/code, links (href annotation), bullet + numbered lists, inline images - and the serialized Portable Text matches the shape consumed by components/news/PortableTextRenderer.tsx and produced by data/sample-news.ts.",
    "Author and Category pickers list Sanity docs and store proper reference objects {_type:'reference', _ref}.",
    "Featured and inline images upload via client.assets.upload (as the logged-in user, no master token) and are stored as image objects with asset reference + alt (+ caption for inline).",
    "Client-side validation blocks save when required fields are missing or limits exceeded.",
    "No new npm dependency added (contentEditable/structured editor is hand-rolled); if unavoidable, added to package.json AND package-lock.json."
  ],
  "verification": [
    "Static review of serialized output against PortableTextBlock (lib/sanity-queries.ts), PortableTextRenderer.tsx component map, and data/sample-news.ts textBlock/buildBody: block _type/style, span marks, markDefs link _key linkage, list listItem+level.",
    "Confirm reference objects and image objects match sanity/schemas/article.ts field definitions.",
    "Confirm all interactive components have 'use client' and imports resolve to existing deps/files.",
    "Rely on GitHub Actions CI for next build + next lint; ensure no unescaped JSX entities, no unused vars, and <img> usage is either next/image or has a deliberate eslint-disable-next-line @next/next/no-img-element with a reason."
  ],
  "blocked_reason": null,
  "findings": "Implemented additively on branch feat/admin-editor. Delivered: app/admin/articles/new/page.tsx and app/admin/articles/[id]/edit/page.tsx (both client components rendering the shared editor; edit page url-decodes params.id and passes it as documentId). components/admin/ArticleEditor.tsx (Studio-like two-column layout: main column = Title, auto-derived editable Slug via slugify() mirroring Sanity - lowercase/strip non-url chars/spaces->hyphens/maxLength 96, Excerpt with live 0/300 counter + maxLength, Body via RichTextEditor, repeatable FAQs; right sidebar = Publish panel with Publish/Save draft buttons, Featured Image uploader (alt only), Author + Category ReferencePickers, Published At datetime-local, SEO Title 0/70 + SEO Description 0/160 counters). Edit mode loads via GROQ coalesce(drafts.<id>, <id>) with the authenticated admin client and hydrates all fields incl body (parseBody), table blocks, images, author/category refs, FAQs. Client-side validate() mirrors sanity/schemas/article.ts: title/slug/excerpt(<=300)/author/category/publishedAt required, seoTitle<=70, seoDescription<=160, FAQ question<->answer paired; inline errors + Publish disabled until valid. onSave(model, mode:'draft'|'publish') hook exposed but NOT implemented (FEAT-003); when absent, buttons show a 'saving arrives in FEAT-003' note and the built ArticleDraft is validated. components/admin/RichTextEditor.tsx (hand-rolled, ZERO new deps): chose a STRUCTURED BLOCK-LIST editor (each block a typed row mapping 1:1 to Portable Text) over a single free-form contentEditable for deterministic, verifiable exact-shape output; text rows use a small per-block UNCONTROLLED contentEditable whose innerHTML is set once on mount (useEffect [] + eslint-disable exhaustive-deps) and serialized to spans on input/blur (domToSpans/spansToHtml) to avoid caret jumps; toolbar supports Paragraph/H2/H3/H4/Quote styles, bullet+number lists (listItem+level:1), Bold/Italic/Underline/Code marks (execCommand + manual <code> wrap), Link (prompt->createLink, stored as link markDef via serializer), inline image insert, block move up/down + delete; renders a read-only summary + removal for existing tableBlock rows and exposes an optional onInsertTable toolbar action so FEAT-003 can drop in the TableEditor cleanly. components/admin/ReferencePicker.tsx (author/category selects, stores {_type:'reference',_ref}). components/admin/ImageUploader.tsx (featured + inline; uploads via getAdminClient().assets.upload('image', file) AS THE LOGGED-IN USER - no master token). All admin client callers now use getAdminClient() (per-user token-aware) instead of the static adminSanityClient export.\n\nREVIEW REMEDIATION (2026-09-03-171500-review.md):\n- RICH TEXT MARKS (Issue #3): Added a normalization pass in domToSpans: each span's marks array is sorted deterministically (decorators alphabetical first, then link keys) and deduplicated. Adjacent spans with identical sorted marks are MERGED into a single span, reducing span noise and bringing output closer to Studio's shape.\n- BR HANDLING (Issue #4): <BR> elements are now DROPPED entirely instead of emitting a {text:'\\n'} span. Sanity does not model hard breaks as literal newline text, and the public renderer (PortableTextRenderer.tsx) would display them as visible whitespace, so dropping them avoids shape divergence.\n- CI READINESS (Issues #5 and #6): Edit page params.id synchronous access is the standard Next 14 'use client' pattern (matches bank-routing-numbers pages in the codebase); SanityClient type import from @sanity/client v8 verified. All callers migrated from adminSanityClient to getAdminClient() which uses withConfig({token}) - a standard v8 API. No unused vars introduced. JSX entities properly escaped (&hellip;, &rsquo;, &ldquo;, &rdquo;, &gt;).\n\nV2 REVIEW REMEDIATION (2026-09-03-172000-review.md, single confirmed issue): INLINE BODY IMAGES NOW RENDER PUBLICLY. The editor+ImageUploader let authors insert/upload inline images that serialize to Portable Text {_key,_type:'image',asset:{_ref,_type:'reference'},alt?,caption?} blocks in article.body, but components/news/PortableTextRenderer.tsx previously registered only a types.tableBlock handler and NO types.image handler, so @portabletext/react rendered those blocks as nothing on the public article page. FIX (additive only): added a types.image component to the existing PortableTextRenderer components.types object, alongside the untouched tableBlock handler. It returns null when asset._ref is missing, otherwise renders via next/image (Image from 'next/image') using the project's existing urlFor builder from lib/sanity (urlFor(value).width(1200).fit('max').auto('format').url()) with alt={value.alt ?? ''}, and renders value.caption as a <figcaption> using the same editorial styling as tableBlock (figure my-10 + figcaption mt-3 text-caption text-ink-muted dark:text-ink-inverse-muted). next/image is lint-safe (avoids @next/next/no-img-element) and cdn.sanity.io is already whitelisted in next.config.js remotePatterns; no eslint-disable needed. Studio-authored inline images share the exact same shape, so they benefit too. The existing tableBlock/block/mark/list/listItem handlers and the TableValue interface were NOT changed; a small local ImageValue interface was added for typing the handler value, consistent with TableValue. No other protected file changed (app/studio/*, sanity.config.ts, sanity/schemas/*, lib/sanity.ts, lib/sanity-queries.ts untouched). SECONDARY (non-blocking): per-user token capture still depends on Sanity returning the token as the sid/token query param after ?type=token hosted login; both param names are handled, but this needs a real-browser smoke test that is not possible in the no-install sandbox.",
  "verification": {
    "static_review": "PASS (no local build possible) - imports resolve to package.json deps (@sanity/client, @sanity/image-url via lib/sanity urlFor) and existing files (@/lib/portable-text, @/lib/admin-types, @/lib/sanity-admin, @/lib/sanity, @/lib/sanity-queries); 'use client' on ArticleEditor/RichTextEditor/ReferencePicker/ImageUploader + both pages; single <img> has deliberate eslint-disable @next/next/no-img-element with reason; contentEditable mount-only innerHTML has eslint-disable react-hooks/exhaustive-deps with reason.",
    "shape_diff": "PASS - reference objects {_type:'reference',_ref} match article.ts author/category; featured image {_type:'image',asset ref,alt} and inline image (+caption) match article.ts mainImage/body image; body blocks/marks/lists/link markDefs/tableBlock produced through lib/portable-text.ts builders which FEAT-001 already diffed against data/sample-news.ts, tableBlock.ts and PortableTextRenderer.tsx.",
    "build_lint": "NOT RUN LOCALLY (environmental - no next/tsc/eslint binary, npm registry unreachable, @sanity/client type folder empty). Deferred to GitHub Actions CI."
  }
}
